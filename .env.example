# Database Configuration
# PostgreSQL connection string for Neon or any PostgreSQL database
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://username:password@your-host.neon.tech/parkshare?sslmode=require

# Server Configuration
# Host address to bind the server (default: 127.0.0.1)
# Use 0.0.0.0 to accept connections from any network interface
HOST=127.0.0.1

# Port for the Express server (default: 5000)
PORT=5000

# Node Environment
# Options: development, production
NODE_ENV=development

# Data Seeding Configuration (Optional)
# Google Maps API key for seeding parking lots/garages
# Get your API key at: https://console.cloud.google.com/apis/credentials
# Free tier: $200/month credit (≈ 6,250 free requests/month)
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
